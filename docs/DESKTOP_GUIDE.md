# Desktop Guide (Web-First)

Use the web app as the primary desktop workflow:

1. Open a terminal in the repository root.
2. Run `pnpm --filter @asst/web dev`.
3. Open `http://localhost:3000` in your browser.
4. Use `/dashboard/console` for interactive operations.
